version: '3.4'

services:
  fc.codeflix.catalog.db:
    container_name: catalog_db_e2e
    hostname: catalog_db_e2e
    image: mysql
    ports :
      - "33060:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root